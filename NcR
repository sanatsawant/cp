ll ncr(int n,int r){
    if(r>n) return 0;
    r=min(r,n-r);

    ll ans=1;
    for(int i=1;i<=r;i++){
        ans = (ans * n) % M;
        n--;
        ans = (ans * powm(i,M-2)) % M;
    }
    return ans;
}
