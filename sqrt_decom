class NumArray {
public:
    vector<int> b;
    vector<int> v;
    int n;
    int blocksize;
    NumArray(vector<int>& v) {
        this->v=v;
        n=v.size();
        blocksize=ceil(sqrt(n));
        b.resize(blocksize,0);

        for(int i=0;i<n;i++){
            int blockno=i/blocksize;
            b[blockno]+=v[i];
        } 
    }
    
    void update(int index, int val) {
        int blockno=index/blocksize;
        b[blockno]+=(val-v[index]);
        v[index]=val;
        
    }
    
    int sumRange(int left, int right) {
        int startblock=left/blocksize;
        int endblock=right/blocksize;
        int ans=0;
        if(startblock == endblock){
            for(int i=left;i<=right;i++) ans+=v[i];
        }
        else{
            for(int i=left;i<=(startblock+1)*blocksize-1;i++) ans+=v[i];
            for(int i=startblock+1;i<=endblock-1;i++) ans+=b[i];
            for(int i=endblock*blocksize;i<=right;i++) ans+=v[i];
        }
        return ans;
    }
};

//opyimized SQRT decom (minor optimizations while loop better here)
int n=Q.size();
        int k=220;

        int blocks=(n+k-1)/k;
        vector<map<ll,int>> b(blocks);
        vector<ll> lazy(blocks);
        for(int i=0;i<q.size();i++){
            b[i/k][q[i]]++;
        }
        vector<int> ans;
        for(auto& x:que){
            if(x[0]==1){
                int l=x[1],r=x[2],val=x[3];

                while(l%k != 0 && l<=r){
                    b[l/k][q[l]]--;
                    q[l]+=val;
                    b[l/k][q[l]]++;
                    l++;
                }
                while(r%k!=k-1 && l<=r){
                    b[r/k][q[r]]--;
                    q[r]+=val;
                    b[r/k][q[r]]++;
                    r--;
                }
                if(l<r){ 
                    for(int i=l/k;i<=r/k;i++) lazy[i]+=val;
                }
            
            }
